# keyboard_leds_toggle

## to view devices:
# brightnessctl -l

# to run manually:
# brightnessctl -d $(cat $XDG_CONFIG_HOME/hardware/keyboard_leds_id) set 0
# brightnessctl -d $(cat $XDG_CONFIG_HOME/hardware/keyboard_leds_id) get 

LEDS=$(brightnessctl -d $(cat $XDG_CONFIG_HOME/hardware/keyboard_leds_id) get)
if [[ "$LEDS" == 0 ]] then 
	brightnessctl -d $(cat $XDG_CONFIG_HOME/hardware/keyboard_leds_id) set 1
else 
	brightnessctl -d $(cat $XDG_CONFIG_HOME/hardware/keyboard_leds_id) set 0
fi